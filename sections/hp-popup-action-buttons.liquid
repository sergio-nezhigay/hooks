<style>
  .hp-action-buttons {
    padding: 30px;
    display: flex;
    justify-content: center;
    align-items: center;
    gap: 20px;
  }
  .action-button-heading {
    text-align: center;
    color: #ed8177;
    font-size: 26px;
  }
</style>
<div class="page-width section-{{section.id}}">
  {% if section.settings.heading %}
    <div class="action-button-heading">{{ section.settings.heading }}</div>
  {% endif %}
  <div class="hp-action-buttons">
    {% if section.settings.button1 %}
      <a class="button primary-btn custom_popup_trigger" href="{{ section.settings.button_1_url}}">{{ section.settings.button1 }}</a>
    {% endif %}
    {% if section.settings.button2 %}
      <a class="button secondry-btn" href="{{ section.settings.button_2_url }}">{{ section.settings.button2 }}</a>
    {% endif %}
  </div>
</div>

{% schema %}
  {
    "name": "HP Popup Action Buttons",
    "settings": [
      {
        "type": "text",
        "id": "heading",
        "label": "Heading"
      },
      {
        "type": "text",
        "id": "button1",
        "label": "Button-1"
      },
      {
        "type": "url",
        "id": "button_1_url",
        "label": "Button 1 Url"
      },
      {
        "type": "text",
        "id": "button2",
        "label": "Button-2"
      }, {
        "type": "url",
        "id": "button_2_url",
        "label": "Button 2 Url"
      }
    ],
    "presets": [
      {
        "name": "HP Popup Action Buttons"
      }
    ]
  }
{% endschema %}